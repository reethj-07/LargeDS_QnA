# Demo video

**Status:** Record when ready — not required to finish code/docs.

Record a **Loom** or local MP4 showing:

1. `python scripts/ingest.py` (or mention prebuilt indices)
2. `python -m src.ui.app` — at least **one complex query** (aggregation + retrieval)
3. **One failure case** (e.g., empty SQL, low critic score, or insufficient evidence)

Paste the link here for your submission:

- Demo URL: _add your link_
